doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body 
        h1 Esta es la pagina de login
        div.botoncrea3
            h1 Movie App
            form#formlogin(action="/login" method="POST")
                h1 Logeate
                label(for='usuario') Usuario:
                input#tituloq(name='usuario' value='')
                label(for='contrasena') Password:
                input#image(name='contrasena' value='') 
                input#logearse(type='submit' value='Log in')
        div.google
            h1 Autenticacion con google
        script(src="./js/main.js")